# Dependências (reinstaladas dentro da imagem via npm ci)
node_modules

# Artefatos de build locais
dist
build

# Variáveis de ambiente / segredos locais
.env
.env.*
!.env.example
!.env.local.example

# Logs e caches
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.vite
.cache

# Git e metadados
.git
.gitignore

# Editor / SO
.DS_Store
.vscode
.idea

# Docker (não precisa entrar no contexto de build da própria imagem)
Dockerfile
.dockerignore
